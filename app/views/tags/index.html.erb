<% @tag.each do |tag| %>
<article>
  <p><%= tag.name %></p>
  <span class="destroy"><%= link_to 'Destroy', tag_path(tag), method: :delete, data: { confirm: 'Are you sure?' } %></span>
</article>
<hr>
<% end %>